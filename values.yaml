name: rhbk

db:
  create: false
  vendor: postgres
  host: postgres-db
  usernameSecret:
    name: keycloak-db-secret
    key: username
  passwordSecret:
    name: keycloak-db-secret
    key: password

http:
  tlsSecret: tls-secret
hostname:
  hostname: rhbk.apps.cluster-g8jmf.dynamic.redhatworkshops.io

image:
  custom: false
  image: ""

replicaCount: 1
